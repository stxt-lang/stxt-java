STRUCT Constants:
    // Carácter que marca el inicio de un comentario en STxT
    CONST COMMENT_CHAR = '#'

    // Número de espacios que equivalen a un nivel de indentación
    CONST TAB_SPACES = 4

    // Carácter de tabulación
    CONST TAB = '\t'

    // Carácter de espacio
    CONST SPACE = ' '

    // Separador clave/valor de un nodo
    CONST SEP_NODE = ':'

    // Codificación por defecto de los documentos STxT
    CONST ENCODING = "UTF-8"

    // Nombre reservado para los documentos de namespace
    CONST NAMESPACE = "Namespace"
